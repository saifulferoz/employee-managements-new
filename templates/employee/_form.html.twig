{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-block btn-primary col-sm-2 float-left">{{ button_label|default('Save') }}</button>
    <div class="float-left col-sm-2"><a href="{{ path('employee_index') }}" class="btn btn-block btn-info">Back</a></div>
{{ form_end(form) }}
